import React from 'react'

function modal(groups) {
  return (
    <div>
      {
        groups.map((group)=>(
            <div>
                <h2>test</h2>
            </div>
        ))
      }
    </div>
  )
}

export default modal
